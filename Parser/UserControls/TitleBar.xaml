<UserControl x:Class="RetailCorrector.Parser.UserControls.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="35" Width="644" Background="#aaaaaa"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#aaaaaa"/>
            <Setter Property="Foreground" Value="#cccccc"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{Binding Foreground, RelativeSource={RelativeSource Self}}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="False">
                    <Setter Property="Background" Value="#aaaaaa"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Canvas Width="644" >
        <Button Canvas.Left="609" Click="CloseApp" Foreground="Red">
            <Button.Content>
                <Canvas Margin="-1,-1">
                    <Line X1="7.5" Y1="7.5" X2="-7.5" Y2="-7.5" Stroke="White" StrokeThickness="1"/>
                    <Line X1="-7.5" Y1="7.5" X2="7.5" Y2="-7.5" Stroke="White" StrokeThickness="1"/>
                </Canvas>
            </Button.Content>
        </Button>
        <Button Canvas.Left="574" Click="MinApp">
            <Button.Content>
                <Canvas  Margin="-1,-1">
                    <Line X1="-7" Y1="6" X2="7" Y2="6" Stroke="White" StrokeThickness="1"/>
                </Canvas>
            </Button.Content>
        </Button>
        <Label Height="35" Width="489" Canvas.Left="85" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
            <TextBlock Text="{Binding PageTitle, StringFormat='RetailCorrector: \{0\}'}" 
                       FontWeight="Medium" />
        </Label>
        <!--Button Canvas.Left="599" Canvas.Top="9.5">
            <Button.Content>
                <Canvas Width="14" Height="14" Margin="-1,-1">
                    <Line X1="0" Y1="11" X2="14" Y2="11" Stroke="White" StrokeThickness="1"/>
                </Canvas>
            </Button.Content>
        </Button-->
        <Button Width="15" Click="OpenMenu">
            <Button.Content>
                <Canvas Width="3" Height="15" Margin="-1,-1">
                    <Ellipse Width="3" Height="3" Fill="{Binding MenuColor}"/>
                    <Ellipse Width="3" Height="3" Fill="{Binding MenuColor}" Canvas.Top="6"/>
                    <Ellipse Width="3" Height="3" Fill="{Binding MenuColor}" Canvas.Bottom="0"/>
                </Canvas>
            </Button.Content>
        </Button>
        <Button Canvas.Left="15" Click="ToPrepPage">
            <Button.Content>
                <Canvas Width="20" Height="15" Margin="-1,-1">
                    <Line X1="7" Y1="0" X2="-1" Y2="7.85" Stroke="{Binding PrepPageColor}" StrokeThickness="1"/>
                    <Line X1="7" Y1="15" X2="-1" Y2="7.15" Stroke="{Binding PrepPageColor}" StrokeThickness="1"/>
                    <Line X1="0" Y1="7.5" X2="20" Y2="7.5" Stroke="{Binding PrepPageColor}" StrokeThickness="1"/>
                </Canvas>
            </Button.Content>
        </Button>
        <Button Canvas.Left="50" Click="ToNextPage">
            <Button.Content>
                <Canvas Width="20" Height="15" Margin="-1,-1">
                    <Line X1="13" Y1="0" X2="21" Y2="7.85" Stroke="{Binding NextPageColor}" StrokeThickness="1"/>
                    <Line X1="13" Y1="15" X2="21" Y2="7.15" Stroke="{Binding NextPageColor}" StrokeThickness="1"/>
                    <Line X1="0" Y1="7.5" X2="20" Y2="7.5" Stroke="{Binding NextPageColor}" StrokeThickness="1"/>
                </Canvas>
            </Button.Content>
        </Button>
    </Canvas>
</UserControl>
